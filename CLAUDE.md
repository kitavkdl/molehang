# 몰래항 (Molehang) — 프로젝트 규칙집

이 파일은 **이후 모든 세션이 반드시 먼저 읽어야 하는 규칙집**이다.
아래 규칙은 취향이 아니라 제약이다. 어기면 롤백 대상.

---

## 1. 게임 한 줄 정의

지루한 시간(수업·회의) 동안 알아서 쌓이는 자원을 틈틈이 수거해 배를 조립하는 유휴(idle) 게임.
**현재 데모는 "조립 전 단계"까지만 존재한다.**

## 2. 현재 데모 범위

1. **무한의 바다** 위에 배 한 척이 떠 있는 3D 씬 (스카이돔 / 바다 / 수평선 섬 / 구름 / 새 / 배)
2. 코어 루프: 시간 경과 → 자원 자동 축적(상한 O, 오프라인 축적 O) → 수거 버튼 → 로그 적재 → 기록 시트
3. 수거 피드백: 배 주위 **물 위에 떠 있는 고철 조각**(강판+금덩이+녹슨 봉, motes.ts)이
   갑판 상자로 빨려드는 연출 + 스케일 바운스. 고철을 공중에 띄우지 말 것 — 허공의
   파티클은 정체불명으로 읽혀서 수면으로 내렸다.
4. **파츠 & 시크릿 전직**: 수거한 고철로 뽑기를 돌리면 무작위 파츠가 나오고
   **전부 자동 장착**된다. 구성에 따라 숨은 칭호가 열린다
   (이끼 10개 → 이끼 유령선, 엔진 8개 → 폭주 기관선 …)
5. 첫 방문 튜토리얼 6스텝
6. 모바일 세로 / PC 가로 두 레이아웃
7. **선단(협동)**: 초대 코드로 묶이면 같이 접속해 있는 동안 축적이 빨라지고,
   친구가 수거하면 그 몫의 일부가 **고철 배당**으로 내 지갑에 들어온다
8. **망원경**: 배율 슬라이더로 멀리(망망대해)부터 가까이(부품 하나)까지 본다.
   확대한 상태에서는 끌어서 화면을 옮긴다 — **돌리지는 못한다**
9. **항해모드**: WASD·방향키·화면 끌기로 배를 몬다. 가끔 암초가 나오고,
   부딪히면 벌점 없이 튕겨나며 **따개비가 붙는다**. §4.9
10. **부품 효과 & 방치 컨텐츠**: 부품마다 생산 외 효과(상한·할인·수거·속도)가 있고,
    낮은 확률로 밸런스 붕괴 부품(황금 오리)이 나온다. 오래 접속하지 않으면
    이끼·갈매기 둥지·유령이 저절로 붙는다. §4.10
11. **선장 아바타**: 모자·옷 색을 고르면 갑판 위에 작은 선장이 선다.
    선단으로 같이 접속해 있는 동안에는 선원들 아바타도 내 갑판에 나란히 선다. §4.11

### 범위 변경 이력 (읽고 넘어갈 것)

- 최초 데모는 "조립 전 단계까지"였고 부품·소켓·전직이 **금지**였다.
- 이후 사용자가 명시적으로 요청해 **파츠와 시크릿 전직이 범위 안으로 들어왔다.**
  바이럴(스크린샷 공유)이 이 기능의 목적이다.
- 바다는 "하늘에 떠 있는 원반"이었으나 **무한의 바다로 교체**됐다. 원반으로 되돌리지 말 것.
- 카메라가 고정 구도 하나였으나 **망원경(배율 + 이동)** 이 들어왔다. §4.8.
- 배치 커스텀에 **접촉 규칙**이 붙었다. 허공에 뜬 부품은 이제 만들 수 없다. §4.7.
- 사용자가 선택해 **고철 경제로 개편**됐다: 수거는 부품이 아니라 **고철**을 주고,
  고철로 3등급 뽑기를 돌린다(§4.4). 같은 개편에서 선단 배당이 부품 → **고철 12%** 로
  바뀌었고(§4.3), **밤 어둠·등불**이 들어왔으며(§3.2), 칭호 조건이 재조정됐다(엔진 8개).
- 사용자가 명시적으로 요청해 **로그인(이메일 6자리 코드)·계정당 여러 배**가 들어왔다.
  게스트가 기본이고 로그인은 선택. 이후 게스트 저장은 **sessionStorage** 로 바뀌었다. §5.
- 사용자가 명시적으로 요청해 **항해모드(WASD + 암초)** 와
  **부품 효과·저확률 밸런스 붕괴 부품·방치 컨텐츠**가 범위 안으로 들어왔다. §4.9 / §4.10.
- 사용자가 명시적으로 요청해 **선장 아바타(갑판 위 선원들)** 가 들어왔다. §4.11.
  같은 요청에서 돌림판이 CSS transition → rAF 직접 구동으로 바뀌었다(§4.12) — 되돌리지 말 것.
- 사용자가 명시적으로 요청해(slowroads.io 레퍼런스) 항해모드가 **크루징**으로 보강됐다:
  스쳐 가는 풍경 소품(부표·유목·바위기둥·모래섬), 소품의 등장/퇴장 연출(솟아오르기·
  가라앉기 + 포말 스플래시), 항적(wake), 속도계. **정박하면 소품은 전부 가라앉아
  사라진다** — "그대로 둔다"였던 옛 규칙을 사용자가 명시적으로 뒤집었다. §4.9.
- 사용자가 명시적으로 요청해 **부품-항해 상호작용**이 들어왔다: 엔진 불꽃·굴뚝 연기·
  돛 부풀기·외륜 회전, 그리고 **무게(heft)** — 무거운 배는 느리고 물에 더 잠긴 채
  달린다(가라앉지는 않는다). §4.9.
- 사용자가 명시적으로 요청해 **항해 회전**이 들어왔다: 배가 가려는 방향으로
  뱃머리를 돌리고(heading), 카메라는 선미 뒤를 따라도는 체이스 캠. 추력은 뱃머리
  정렬(cos²)에 비례 — **완전히 돌아야 최고 속도**. 우클릭 드래그 자유 회전.
  "회전은 없다"였던 옛 규칙은 **항해에 한해** 뒤집혔다 — 정박 망원경은 여전히 회전 없음. §4.9.
- 사용자가 명시적으로 요청해 **선체 손상**이 들어왔다: 암초에 부딪히면 내구도가
  깎여 느려지고, 정박하면 아문다. "충돌 벌점 없음"의 부분 개정 — 침몰·비용은 여전히 없다. §4.9.
- 사용자가 명시적으로 요청해 **고철이 리디자인**됐다: 공중 궤도를 돌던 추상 결정 →
  물 위에 떠다니는 고철 조각(강판+금덩이+녹슨 봉), 지갑 아이콘도 전용 SVG 마크.
  같은 요청에서 **부력**이 들어왔다: 빈 배는 높이 뜨고 heft 가 쌓일수록 가라앉는다. §4.9.
- 사용자가 명시적으로 요청해 자리 규칙이 바뀌었다: **이끼 0칸, 따개비·둥지·유령 0.1칸.** §4.5.

### 여전히 만들지 말 것

수익화 · 파츠 선택/판매/강화 UI · 랭킹/경쟁 요소.
(로그인은 이후 사용자 요청으로 들어왔다 — 게스트가 기본이고 로그인은 선택이다. §5)
**파츠는 고를 수 없다** — 나온 건 전부 붙는다. 그 강제성이 이 게임의 유머다.
협동은 들어왔지만 **경쟁은 아니다.** 남과 비교해 뒤처지는 느낌을 만드는 장치를 넣지 말 것.

---

## 3. 아트 디렉션 — 가장 중요, 엄격히 준수

> 레퍼런스 느낌: 모바일 게임 **Hot Slide**. 플랫 셰이딩 로우폴리, 텍스처 없는 단색 면,
> 밝고 채도 높은 색, 깨끗한 미니멀 UI.

### 3.1 팔레트 — 단일 소스 규칙

- **모든 색은 `src/style/palette.ts` 에서만 나온다.** 예외 없음.
- 씬 색, 하늘 4단계 보간 색, 라이트 색, 파츠 색, UI 색, CSS 변수까지 전부 이 파일이 출처다.
- **`.ts` / `.css` / `.glsl` / `.html` 어디에도 하드코딩된 hex·rgb·hsl 리터럴을 쓰지 않는다.**
  새 색이 필요하면 팔레트에 추가하는 게 아니라, **기존 15색 중에서 고른다.**
- 코어 팔레트는 **15색 고정**. (12 → 15 로 한 번 늘렸다: 파츠에 금속·이끼·녹이 필요했고
  기존 12색은 전부 바다·하늘 계열이라 부품이 배경에 묻혔다. 이유 없이 더 늘리지 말 것.)
- 두 팔레트 색 **사이를 보간**하는 것은 허용된다(하늘 그라데이션, 섬 헤이즈, 바다 중간 계단).
  새로운 색을 자유롭게 만드는 것과는 다르다.
- CSS는 `applyThemeVars()` 가 런타임에 주입하는 `--mh-*` 변수만 사용한다.

### 3.2 머티리얼

- **텍스처 금지.** 이미지 로딩, UV 매핑, 노멀맵, 환경맵 전부 금지.
- 메시 머티리얼은 **`src/scene/flat-material.ts` 의 `flat(팔레트키)` 하나만 쓴다.**
  ```ts
  import { flat } from '../scene/flat-material.ts';
  const material = flat('timber');   // 팔레트 키만 받는다
  ```
- `MeshStandardMaterial` / `MeshPhysicalMaterial` 사용 금지 (PBR 광택은 이 아트 방향과 충돌).
- 셰이더를 쓰더라도 결과는 **단색 면 + 계단식 톤 스텝**이어야 한다. 부드러운 스페큘러 금지.

#### 왜 Lambert 를 안 쓰는가 (되돌리지 말 것)

`MeshLambertMaterial` 은 광원 반대편 면이 그대로 어두워진다. 실제로 이 프로젝트에서
선체가 진흙색, 돛이 회색으로 죽어 아트 방향이 무너졌다. `flat()` 은 조명 모델을 직접 써서
**면 색이 팔레트 색의 80% 아래로 절대 안 내려가게** 보장한다. 3단 계단 셰이딩, 스페큘러 없음.

예외 하나 — **밤 어둠**(사용자 요청): nightness 에 따라 배가 어둠에 잠기고,
등불 부품(uLamp)이 원래 색을 되찾게 한다. 조명 모델의 결함이 아니라
"등불을 다는 이유"를 만드는 의도된 연출 레이어다. 지우지 말 것.
화면 평균 휘도 기준(§3.4)은 이 상태로도 지켜져야 한다 — `npm run shots` 가 검사한다.

`flat()` 에 대해 알아야 할 두 가지:

1. **면 노멀을 화면 미분(`dFdx/dFdy`)으로 구한다.** 정점 노멀이 필요 없어서 생성 지오메트리에
   `computeVertexNormals()` 를 안 불러도 된다.
2. **`side: DoubleSide` 를 절대 바꾸지 말 것.** 노멀을 카메라 쪽으로 뒤집어 쓰기 때문에
   와인딩이 어긋난 면도 정상적으로 그려진다. `FrontSide` 로 바꾸면 돛·갑판이 통째로 사라진다.

조명 uniform 은 전 머티리얼이 공유한다 — 프레임마다 `updateFlatLighting(skyState)` 한 번.

### 3.3 라이팅

- **방향광 1개 + 헤미스피어 라이트 1개.** 그 이상 추가 금지.
- 그림자는 섀도우맵 대신 **연한 단색 블롭**(반투명 단색 평면)으로 표현한다.
- 톤매핑은 `NoToneMapping`. ACES 등은 채도를 죽이므로 금지.

### 3.4 밝기 — 실패 기준

- **전체적으로 밝고 개방감 있게. 어둡거나 칙칙하면 실패다.**
- 밤 시간대도 "어두운 화면"이 아니라 **밝은 남보라 트와일라잇**이다.
- 자동 검증 기준: 4개 시간대 전부 화면 평균 휘도 **0.34 이상** (`npm run shots` 가 검사).

## 4. 모바일 우선

- **세로 화면 기준**으로 디자인·검증한다. 데스크톱은 세로 레이아웃이 가운데 오는 정도로만 대응.
- 배가 화면 대부분을 차지한다. 하단에 **수거 버튼 하나**, 나머지 정보는 **아래에서 올라오는 시트**로.
- 상시 노출 UI 최소화: 상단 자원 표시 + 하단 버튼. 그 외에는 시트 안으로.
- 터치 타겟 최소 44px, `safe-area-inset` 반영, 더블탭 줌 차단.

### 4.1 구도 — `src/scene/framing.ts` 가 단일 출처

카메라 값을 아무 데나 하드코딩하지 말 것. 하늘 요소 전부가 이 값에서 자기 높이를 계산한다.

화면을 나누는 선은 **수평선 하나**다. 위는 하늘, 아래는 바다, 경계에 섬 실루엣.

- 바다는 반경 320의 방사형 격자지만 **가장자리는 절대 안 보인다** — 34~165 사이에서
  하늘 색(`skyBottom`)으로 완전히 흐려지기 때문. 그래서 "무한"으로 읽힌다.
  헤이즈 색은 반드시 하늘 밑동 색과 **같아야** 한다. 다르면 수평선에 이음매가 생긴다.
- 구름·새는 `skyY(깊이, 고도°)` 로 배치한다. 절대 y 를 손으로 넣지 말 것.
- 섬은 밑동이 헤이즈 색, 위로 갈수록 실루엣 색인 세로 그라데이션이다.
  통짜 단색으로 두면 종이 오려 붙인 것처럼 뜬다.
- **세로 화면의 가로 시야각은 ±9° 뿐이다.** 해/달 원반을 좌우로 조금만 틀어도 화면 밖으로 나간다.
  그래서 원반 방향(`PHASE_DISC_DIR`)은 조명 방향(`PHASE_SUN_DIR`)과 **일부러 분리**돼 있다.
  조명은 높이 두고, 원반만 수평선 바로 위에 낮게 걸어 물 위로 반사 길이 길게 떨어지게 한다.
- **파장은 배 길이(6.3)보다 짧게.** 카메라가 수면에 가까워서 파장이 길면
  근경이 거대한 색 얼룩이 된다. 실제로 한 번 그렇게 만들었다가 되돌렸다.

### 4.1.1 구도는 **기본 배율(1배)** 기준이다

위의 모든 수치와 규칙은 배율 1 에서의 이야기다. 망원경이 그 위에 얹힌다(§4.8).
구도를 손볼 때는 반드시 **배율 양 끝**(`?zoom=0.4`, `?zoom=4`)까지 같이 확인할 것 —
1배에서 멀쩡한 값이 최소 배율에서 섬 끝이나 구름 경계를 드러내는 일이 있다.

### 4.2 커스텀 셰이더의 함정 (두 번 당했다)

`ShaderMaterial` 의 기본값은 `side: FrontSide` 다. 코드로 만든 지오메트리는 와인딩이
일정하지 않아서 **통째로 컬링돼 사라진다.** 돛·갑판이 사라졌고, 섬이 사라졌다.
씬에 새 커스텀 셰이더를 넣으면 `side: DoubleSide` 부터 확인할 것.

**framing.ts 나 파도/헤이즈 상수를 건드렸으면 반드시 `npm run shots` 로 전부 다시 볼 것.**

## 4.3 선단 (협동)

설계 원칙 세 가지. 이 셋을 깨는 변경은 하지 말 것.

1. **혼자서도 완결된다.** 선단은 전부 덤이다. 혼자 하는 사람이 막히는 구간이 없어야 하고,
   UI도 혼자일 땐 아예 숨긴다(선단 칩). 결핍을 만들어 친구를 부르게 하지 않는다.
2. **같이 접속해 있는 동안에만** 보너스가 붙는다. 친구를 등록해 두고 방치하는 게 아니라
   같은 시간에 켜 두는 것이 이득이라야 "같이 하는" 게임이 된다.
   → 오프라인 축적(`load()`)에는 배율을 적용하지 않는다. 이걸 바꾸면 설계가 무너진다.
3. **친구의 수거가 나에게도 이득이 된다.** 친구가 수거하면 그 몫의 12%가
   고철 배당으로 내 지갑에 들어온다. 초기에는 부품을 직접 얹어 줬지만,
   경제 개편으로 부품이 자리를 먹게 되면서 남이 내 자리를 말없이 차지하는
   사고가 됐다 — 배당은 고철로만 주고, 그걸로 뭘 뽑을지는 내가 정한다.
   부품을 직접 얹어 주는 방식으로 되돌리지 말 것. (game/crew.ts)

구현: `game/crew.ts`(순수 계산) · `game/crew-session.ts`(코드·이름) · `net/crew-channel.ts`(통신).
지금 채널은 `BroadcastChannel` 이라 **같은 브라우저의 다른 탭끼리만** 이어진다.
다른 기기의 친구는 Supabase Realtime 구현체로 갈아끼우면 된다 — 게임 코드는 안 바뀐다.
UI에도 이 한계를 그대로 적어 두었다. 서버가 붙기 전까지 그 문구를 지우지 말 것.

`?seat=b` 는 저장소를 분리해 같은 기기에서 친구 역할을 하나 더 띄우는 용도다.

## 4.4 경제 — 희소 자원은 **선체 공간**

이 게임의 유일한 진짜 선택이다. 흔들지 말 것.

- 수거는 **고철**을 준다. 고철로 뽑기를 돌리고, 나온 부품은 **반드시 장착**된다.
- 생산량도 부품에서 나온다 → 좋은 부품을 넣으려면 자리를 비워야 하고,
  자리를 비우면 생산이 깎인다. **고철이 아무리 많아도 이 긴장은 안 풀린다.**
- 자리가 모자란 채로 뽑으면 그 자리에서 뺄 부품을 고르게 한다.
  결과가 뜬 동안 닫기 버튼은 잠긴다 — 회피 경로를 만들지 말 것.
- 상한 = 초당 생산량 × 30분. 생산이 늘면 상한도 같이 늘어난다.
- 초당 생산량은 **상시 노출**한다. 교체 결정의 유일한 근거다.

## 4.5 파츠 시스템

- 파츠는 `src/game/parts.ts`(데이터·확률·칭호) 와 `src/scene/part-sockets.ts`(배치) 로 나뉜다.
- **n 번째 파츠는 언제나 자리가 있어야 한다.** 구역 격자가 다 차면 위로 쌓는다.
  같은 종류를 12개 받아도 실패하지 않고 탑이 된다 — 그 기형이 의도다.
- 배치는 (구역, 순번)만 받는 순수 함수라, 같은 인벤토리면 항상 같은 배가 나온다.
- 부품을 추가할 땐 `PART_KINDS` · `PART_INFO` · `build()` 세 곳만 고치면 된다.
  배치는 구역(`zone`)만 지정하면 격자가 알아서 자리를 내준다.
- 자리는 **소수 칸**이 허용된다: 이끼 0칸, 따개비·갈매기 둥지·유령 0.1칸.
  `usedSlots()` 가 0.1 단위로 반올림해 부동소수점 먼지를 턴다. 표시는 `slotsLabel()`.
  따개비류(weight 0)는 `removableKinds()` 에서 빠진다 — 뗄 수 없는 게 유머다.

## 4.6 바다 테마

- 테마는 **같은 15색을 다르게 조합한 것**이다. 새 색을 만들지 않는다.
  이 제약 덕분에 어떤 테마를 뽑아도 게임 전체가 한 팔레트 안에 머문다.
- 바꾸는 것은 바다·하늘·섬 색뿐. 구름과 조명은 기본을 물려받는다
  (전부 갈아끼우면 시간대 4단계가 무너진다).
- 테마 패치는 `src/style/themes.ts` 의 `patch[phase]` 에만 쓴다.
  `palette.ts` 의 `PHASE_COLORS` 는 기본값이므로 테마가 건드리지 않는다.
- 테마를 추가하면 **네 시간대 전부** 확인할 것. 낮만 보고 넣으면 밤에 무너진다.

## 4.7 배치 커스텀

- "자유롭게" 는 **자기 구역 안에서** 자유롭다는 뜻이다. `ZONE_BOUNDS` 밖으로는 못 나간다.
  풀어 주면 부품이 허공이나 선체 안쪽에 박힌다.
- 드래그 좌표는 화면 → 월드 평면 → **배 로컬** 순으로 옮긴다.
  배는 파도에 흔들리고 요(yaw)도 돌아가 있어서 로컬로 되돌리지 않으면 어긋난다.
- 배치 모드에서는 수거·뽑기 버튼을 **화면에서 치운다.** 부품을 끌다 손가락이 닿아
  뽑기가 돌아가면 되돌릴 수 없는 사고가 된다.
- 저장 단위는 `"kind#순번"` → `[x,y,z]`. 비어 있으면 격자 기본 자리.

#### 접촉 규칙 — "닿아 있어야 붙는다"

`src/scene/part-support.ts`. **물리 엔진을 붙이지 않았다** — 3D 런타임 의존성은 three 하나뿐이고(§6),
필요한 것도 강체 시뮬레이션이 아니라 규칙 두 줄이다.

1. 부품은 **무언가에 닿아 있어야** 한다. 허공에 뜬 돛은 만들 수 없다.
   손가락을 하늘로 끌어도 부품은 닿는 자리에서 멈춘다.
2. **그 외에는 아무것도 따지지 않는다.** 무게중심·균형·대칭은 검사하지 않는다.
   대포 여섯 문이 전부 우현에 매달려 배가 뒤집힐 것 같아도 그대로 설치된다 —
   그 기형이 이 게임의 유머다. **균형 검사를 추가하지 말 것.** 배치의 자유가 사라진다.

구현 메모:

- 판정은 축 정렬 상자(AABB) 겹침. 좌표계는 **배 로컬** — 배는 파도에 흔들리므로
  월드로 재면 매 프레임 답이 바뀐다.
- `CONTACT_GAP` 은 0 이 아니다. 0 으로 두면 격자 기본 자리조차 "떠 있음"이 된다.
- 끄는 **내내** 물리를 통과시킨다. 손을 뗄 때만 계산하면 부품이 마지막에 갑자기 뛴다.
- 얹히는 구역(갑판·선미, `restsOnTop`)만 위로 밀어낸다. 현측·돛대 부품은
  선체와 돛대에 파묻혀 있는 게 정상이라, 여기서 밀어내면 배에서 떨어져 나간다.
- 선체 치수는 `hull.ts` 의 `FITTINGS` 하나에서 온다. boat.ts 와 part-support.ts 가
  같은 숫자를 보지 않으면 부품이 돛대 옆에 뜬다.
- **격자 기본 자리는 물리에 넣지 않는다.** 구역 상자에 가두면 §4.5 의 "탑"이 뭉개진다.
  저장된 커스텀 자리만 불러올 때 다시 통과시킨다(`reseat`) — 옛 저장본에 뜬 부품이 남아 있다.

## 4.8 망원경

배율 하나와 이동 둘. `src/scene/framing.ts`(수치) · `src/scene/telescope.ts`(입력).

- **배율은 0.4 ~ 4.** 1 이 §4.1 의 기본 구도다.
- **줄일 때와 키울 때가 대칭이 아니다.** 키울 때는 카메라를 두고 화각만 좁힌다(진짜 망원경).
  줄일 때 화각만 넓히면 어안이 되어 근경 바다가 화면을 먹으므로 카메라를 같이 뒤로 뺀다.
  최소 배율의 목적은 배를 줄이는 게 아니라 **주변을 늘리는** 것이다 — 그게 망망대해다.
- **망원경에 회전은 없다.** 끌면 카메라와 시점이 **같은 양만큼 평행이동**할 뿐이다.
  (회전은 **항해모드에만** 있다 — §4.9 체이스 캠. 사용자가 명시적으로 요청해
  항해에 한해 옛 "회전 없음" 규칙을 뒤집었다. 정박 구도는 여전히 한쪽에서 본다.)
- 이동 범위는 배율 1 에서 0 이고 배율이 오를수록 열린다. 배를 잃어버릴 수 없어야 한다.
- 확대하면 카메라 흔들림을 줄이고 배의 파도 흔들림을 따라간다.
  화각이 좁아지면 같은 흔들림이 화면에서 몇 배가 된다.
- 슬라이더 눈금은 **로그**다. 선형이면 1배 근처에서 미세 조정이 안 된다.

## 4.9 항해모드

`src/scene/voyage.ts`(이동·소품·충돌·스플래시) · `src/scene/wake.ts`(항적) ·
`src/ui/voyage-ui.ts`(껍데기 + 속도계).

- **배는 원점에서 움직이지 않는다. 바다가 흐른다.** 배의 "바다 위 위치"(seaX/seaZ)만
  적분하고, 바다 셰이더는 파도 위상을 그만큼 밀며(ocean.ts `uOffset`),
  암초는 (암초 좌표 − 배 좌표) 자리에 그린다. 덕분에 §4.1 구도·§4.8 망원경·배치가
  전부 그대로 동작한다. 이 구조를 "진짜로 배를 움직이는" 쪽으로 되돌리지 말 것 —
  바다 격자 가장자리(반경 320)가 드러나고 카메라 규칙이 전부 깨진다.
- 파도 위상만 바다 좌표를 쓰고, **헤이즈·반사 길은 화면 기준을 유지**한다.
  수평선과 빛기둥은 카메라에 붙박여 있어야 한다.
- **항해에는 회전이 있다** (사용자 요청으로 옛 규칙을 뒤집음):
  - 배는 가려는 방향으로 **뱃머리를 돌린다**(heading, 회두 속도 제한 · 무거우면 굼뜸).
    추력은 언제나 뱃머리 방향 — 옆으로 미끄러지는 배는 없다.
  - **정렬 속도 규칙**: 추력 = cos²(뱃머리↔목표 각차), 바닥 12%.
    뱃머리가 완전히 돌아야 최고 속도가 난다. 이 곡선을 평평하게 만들지 말 것.
  - 카메라는 **달리기 시작하면** 선미 뒤로 따라 도는 체이스 캠(viewYaw)이다.
    서 있을 때는 돌지 않는다 — 출항 버튼을 누른 순간 화면이 홱 돌면 사고다.
  - 입력(키·끌기)은 **카메라 기준**이다. 화면 위 = 지금 보는 방향.
  - 마우스 우클릭 드래그 = 자유 회전(orbitOffset). 손을 떼면 체이스 자리로 풀린다.
  - 정박하면 뱃머리는 `BOAT_YAW`, 카메라는 0 으로 **서서히** 돌아온다 — §4.1 구도 복귀.
  - 카메라가 사방을 보므로 **구름·수평선 섬은 배 중심의 원 위에** 심어져 있다
    (clouds.ts·islands.ts). 한쪽에만 몰아 두면 뒤돌아본 수평선이 텅 빈다.
- 키는 `e.code`(물리 자리)로 읽는다 — 한글 자판에서도 WASD 가 먹는다.
- 화면 끌기가 타륜이므로 항해 중 망원경 이동은 막는다(blocked). 켤 때 배율을 1로 되돌린다.
- 항해 중에는 수거·뽑기 버튼을 화면에서 치운다 — §4.7 과 같은 이유.
- **충돌하면 선체가 상한다** (사용자 요청으로 "벌점 없음"을 부분 개정):
  내구도(hull 0~1)가 한 번에 0.14 깎이고 최고 속도가 준다(바닥 45% — 멈추지는 않는다).
  **침몰·수리비·게임오버는 없다.** 정박해 있으면 40초에 걸쳐 저절로 아문다.
  게이지는 항해 바에 있다. 튕겨나기와 따개비(weight 0, 자리 0.1)는 그대로다.
  첫 항해의 암초 하나는 반드시 정면에 심는다.
- 항해 속도는 부품 효과(엔진·돛·외륜)로 빨라진다. 상한 +4.
- 바다 소품은 두 부류다(slowroads.io 레퍼런스의 "스쳐 가는 풍경"이 목적).
  부딪히는 것(암초·바위기둥·모래섬, 동시 최대 6)과 **충돌하지 않는 풍경**(부표·유목).
  풍경을 충돌체로 바꾸지 말 것 — 바다가 장애물 코스가 되면 크루징이 죽는다.
- **소품은 갑자기 나타나거나 사라지지 않는다.** 등장은 물속에서 솟아오르고(포말
  스플래시와 함께), 퇴장은 가라앉는다. 화면 밖(130+)으로 벗어난 것만 연출 없이 치운다.
- **정박하면 소품은 전부 차례로 가라앉아 사라진다.** 다음 출항 때 새로 심는다.
  "정박 중에도 남겨 둔다"였던 옛 규칙은 사용자가 명시적으로 뒤집었다 — 되돌리지 말 것.
- 항적(wake.ts)은 포말 조각을 배가 지나온 **바다 좌표**에 떨어뜨린다. 바다가 흐르는
  구조 덕분에 저절로 뒤로 밀려나며 항적이 된다 — 배 기준 좌표로 바꾸면 안 된다.
- **부품이 배의 "달리는 모습"을 바꾼다** (boat.ts `mountVoyageFx`): 엔진·대형 엔진은
  불을 뿜고, 굴뚝·터빈은 연기를 올리고, 돛은 부풀고, 외륜은 돈다. 전부 항해 속도에
  비례해서(drive) 정박하면 잠잠해진다 — 별도 온오프 플래그를 만들지 말 것.
- **무게(heft)는 항해와 흘수에만 영향을 준다** (parts.ts `heft` 필드, 기본값 = slots).
  무거운 배는 최고속도·가속이 깎이고(드래그 바닥 55%) 물에 더 잠긴 채 달리며,
  항해 중 이따금 더 가라앉으려는 출렁임이 얹힌다 — **가라앉지는 않는다.**
  **부력**: 갓 시작한 빈 배는 코르크처럼 높이 뜨고(`0.3·e^(−heft/26)`), heft 가
  쌓일수록 조금씩 가라앉는다(boat.ts). 배가 낮게 시작하도록 되돌리지 말 것.
  경제(생산·상한·할인)와는 완전히 무관하다. 따개비·둥지·유령의 무게(heft)는 0 —
  암초 충돌·방치에 벌점을 만들지 않는 원칙 그대로다. 바꾸지 말 것.

## 4.10 부품 효과 & 방치 컨텐츠

- 효과는 `parts.ts` 의 `effects` 필드 하나로 끝난다: `prodMult`(생산 배율) ·
  `capacity`(상한) · `discount`(뽑기 할인) · `collect`(수거 보너스) · `speed`(항해).
  전부 **인벤토리만 보는 순수 계산**이라 서버 정산과 충돌하지 않는다.
  §5 의 500/s 서버 클램프는 그대로다 — 지우지 말 것.
- 폭주 방지 상한: prodMult ×64, capacity ×8, discount 50%, collect ×4, speed +4.
  **황금 오리(생산 ×2, 대형 풀의 ~2%)는 의도된 밸런스 붕괴다.** 확률을 올리지 말 것.
- `weight: 0` 부품은 뽑기에 안 나온다 — 따개비(암초)·갈매기 둥지(24h 방치)·
  유령 선원(72h 방치) 전용 획득 경로다. 돌림판·'잡동사니 방주' 판정에서도 빠진다.
  이들은 자리를 0.1칸만 먹고, `removableKinds()` 에서 빠져 뗄 수도 없다.
- 방치 성장은 `game/idle.ts` 순수 함수 하나로 판정한다. 게이트웨이가 부팅 때
  `offlineMs()` 를 내주고, 장착은 뽑기와 같은 `install()` 경로를 탄다.
- **이끼는 자리를 아예 안 먹는다**(0칸, 한 번에 최대 2개) — 방치가 자리 초과
  교착을 만들 수 없다. 유령은 배에 **한 명뿐**, 둥지는 총 5개까지.

## 4.11 선장 아바타

`src/game/avatar.ts`(데이터·저장) · `src/scene/avatars.ts`(갑판 피규어) · 시트의 커스터마이즈.

- 아바타는 **배가 아니라 사람의 것**이다. 배 저장(게이트웨이)이 아니라 선단 이름과
  같은 자리(localStorage, `?seat=` 접미사)에 산다. 배를 갈아타도 아바타는 그대로다.
- 옷 색 id 는 **팔레트 키 문자열 그대로**다. 아바타에도 새 색을 만들 길은 없다.
- 아바타는 부품이 아니다 — 자리를 안 먹고, 끌 수 없고, 배치 물리와 무관하다.
  서는 자리(SPOTS)는 상자(z=-1.6)와 돛대(z=0.5)를 피해 고정돼 있다. 정원 = 선단 정원(4).
- 선원 아바타는 **같이 접속해 있는 동안만** 내 갑판에 선다 — §4.3 의 원칙 그대로.
  채널로 들어온 아바타 값은 반드시 `sanitizeAvatar()` 로 거른다 (남의 탭이 만든 값이다).

## 4.12 돌림판 — rAF 직접 구동 (세 번째 사고 후 결론)

- 스핀을 **CSS transition 으로 되돌리지 말 것.** transition 은 시작 상태를 브라우저가
  한 번 그렸는지에 달려 있어, 패널이 hidden 에서 풀리는 프레임과 겹치면 회전이 통째로
  생략되고 원판이 결과 각도로 **순간이동**한다. 리플로우 강제 + rAF 대기로도 간헐적으로
  터졌다. 지금은 매 프레임 각도를 계산해 넣는다(gacha.ts) — 이 계급의 버그가 존재하지 않는다.
- 뽑기 요청이 네트워크를 타는 동안(로그인 상태 1~2초) 원판은 **예열 회전**을 돈다.
  죽은 화면은 "고장"으로 읽힌다.
- `busy` 플래그는 열리는 즉시 선다 — 연타 이중 결제, 요청 대기 중 스크림 탭/Esc 로
  판이 닫히는 사고를 막는다. 결과가 뜬 동안 닫기가 잠기는 규칙(§4.4)은 그대로다.

같은 계급의 규칙 두 개가 더 있다 (전면 사냥에서 나온 결론):

- **등장 연출은 `ui/paint.ts` 의 `afterPaint()` 로만 건다.** hidden 을 풀고 같은
  프레임에 전환 클래스를 붙이면 연출이 통째로 생략된다. rAF 한 번으로는 부족하다.
  시트·뽑기판·계정·튜토리얼·토스트 전부 이 헬퍼를 쓴다.
- **패널의 지연 숨김 타이머는 "그 사이 다시 열렸는지"를 반드시 확인한다.**
  닫기 → 240ms 안에 재열기 → 옛 타이머가 새 판을 숨기는 사고가 뽑기판·계정에서 났었다.
- **게이트웨이 변이는 `Game` 의 직렬화 큐(enqueue)를 통해서만 나간다.**
  로그인 상태에서 수거 연타·선물 배당·따개비 장착이 겹치면 같은 pending 이 두 번
  지갑에 들어가거나 마지막 push 가 이긴다. 새 변이 메서드를 추가하면 enqueue 로 감쌀 것.

## 5. 시간/자원 계산 — 나중에 Supabase로 교체

로컬 구현은 **인터페이스 뒤에** 숨어 있어야 한다. 교체 지점은 이 셋뿐이다.

| 인터페이스 | 파일 | 게스트 | 로그인 |
| --- | --- | --- | --- |
| `MolehangGateway` | `src/game/gateway.ts` | `LocalGateway` (sessionStorage) | `SupabaseGateway` (완료) |
| `CrewChannel` | `src/net/crew-channel.ts` | `BroadcastChannel` (같은 브라우저) | Supabase Realtime (미착수) |
| `TimeOfDaySource` | `src/core/time-of-day.ts` | 기기 로컬 시각 | 그대로 (연출용, 서버 무관) |

게스트 저장은 **sessionStorage** 다 — 탭을 닫으면 게스트 기록도 같이 사라진다
(새로고침·로그인 리로드에는 살아남는다). 계정으로 남기려면 로그인하면 되고, 그때
게스트로 키운 배가 통째로 계정으로 옮겨 간다. localStorage 로 되돌리지 말 것 —
사라진 줄 알았던 게스트 배가 로그아웃 때 되살아나는 사고의 근원이었다.

### 서버가 시간의 권위를 갖는다

로그인 상태에서 축적 정산은 `sync_ship` RPC 가 `now()` 로 한다. 기기 시계를 앞으로
돌려도 소용없다. 생산량은 클라이언트가 보내되 서버가 500/s 로 자른다 —
부품 표를 SQL 에 복제하지 않으면서 시간 조작만은 막는 절충이다.
**이 절충을 없애려면 부품 표를 SQL 로 옮겨야 한다. 그 전까지는 클램프를 지우지 말 것.**

- 축적 계산은 `src/game/accrual.ts` 의 **순수 함수**다. DOM·localStorage·three.js를 import 하지 않는다.
  → 나중에 Supabase Edge Function에 그대로 복사할 수 있어야 한다.
- 게이트웨이 메서드는 전부 `Promise` 를 반환한다(지금 로컬이라도). 네트워크로 바뀔 때 호출부가 안 바뀌게.
- UI/씬 코드가 `localStorage` 를 직접 만지면 안 된다. 반드시 게이트웨이 경유.

## 6. 기술 스택

- Vite + TypeScript + Three.js. **프레임워크(React/Vue 등) 추가 금지.**
- 런타임 의존성은 `three` 와 `@supabase/supabase-js`(로그인·클라우드 세이브, §5) 둘만
  유지한다. 애니메이션/상태관리/물리 라이브러리 추가 금지.
- 빌드 산출물에 이미지·폰트 애셋을 넣지 않는다(텍스처 금지 규칙의 연장). 폰트는 시스템 폰트 스택.

## 7. 개발 / 검증

```bash
npm run dev      # http://localhost:5173
npm run build    # tsc --noEmit + vite build
npm run shots    # Playwright로 4개 시간대 스크린샷 + 밝기 검증
```

### 디버그 쿼리 파라미터

| 파라미터 | 예 | 효과 |
| --- | --- | --- |
| `?hour=` | `?hour=18.5` | 연출 시각 강제 (0~24). 자원 축적에는 영향 없음 |
| `?phase=` | `?phase=dusk` | `dawn` / `day` / `dusk` / `night` 프리셋 |
| `?res=` | `?res=full` | 자원량 강제 (`full` 또는 숫자) |
| `?parts=` | `?parts=engine*12,moss*8` | 파츠 강제 장착 (누적됨) |
| `?zoom=` | `?zoom=0.4` | 망원경 배율 강제 (0.4~4) |
| `?voyage=1` | | 항해모드로 시작 |
| `?away=` | `?away=80` | 방치 시간 강제 (시간 단위) — 이끼·둥지·유령을 바로 본다 |
| `?notutorial=1` | | 첫 방문 튜토리얼 건너뛰기 |
| `?probe=1` | | `window.molehang.sampleLuminance()` 활성화 |

기본 시각 소스는 **접속한 기기의 로컬 시각**이다(`LocalTimeOfDay`). 위 파라미터는 디버그 전용.

**UI/씬을 건드린 세션은 반드시 `npm run shots` 를 돌려 4개 시간대를 눈으로 확인한다.**
스크린샷은 `docs/shots/` 에 저장된다. 밝기 수치만 보고 넘어가지 말 것 —
수치는 통과해도 구도가 깨지거나 면이 사라져 있을 수 있다. **이미지를 직접 열어 볼 것.**

`npm run shots` 첫 실행 전에 한 번: `npx playwright install chromium`
